<pdf>
  <head>
    <style type="text/css">

      body {
        font-family: sans-serif;
        font-size: 9pt;
        margin: 0;
      }

      table {
        width: 100%;
        border-collapse: collapse;
        font-size: 8pt;
      }

      th, td {
        border: 0.2px solid #e0e0e0;
        padding: 3px 4px;
        font-size: 8pt;
      }

      th {
        background-color: #f9f9f9;
        font-weight: bold;
      }

      .no-border td,
      .no-border th {
        border: none;
      }

      .center { text-align: center; }
      .right { text-align: right; }
      .left { text-align: left; }
      .bold { font-weight: bold; }

      h2 {
        margin: 0;
        font-size: 14pt;
      }

      h3 {
        font-size: 9pt;
        margin-top: 10px;
        margin-bottom: 5px;
      }

      p {
        font-size: 8pt;
        margin: 0;
      }

    </style>
  </head>

  <body padding="10mm">

    <!-- Header Section -->
    <table class="no-border" style="margin-bottom: 8px;">
      <tr>
        <td style="width:70%;">
          <strong>${subsidiary.companyName!"Company Name"}</strong><br/>
          ${subsidiary.mainaddress_text!""}<br/>
          TRN: ${subsidiary.federalidnumber}
        </td>
        <td style="width:30%; text-align:right;">
          <@filecabinet nstype="image" src="${subsidiary.logo}" style="width:140px; height:30;" />
        </td>
      </tr>
    </table>

<!-- Title -->
<table class="no-border" style="width:100%; margin-bottom:8px;">
  <tr>
    <td style="text-align:center; width:100%;">
      <h2 style="margin:0;">CUSTOMER PAYMENT RECEIPT</h2>
    </td>
  </tr>
</table>

    <!-- ================= PAYMENT INFO ================= -->
    <table class="section">
      <tr>
        <td><b>Customer:</b> ${record.customer.altname!""}</td>
        <td><b>Date:</b> ${record.trandate!""}</td>
        <td><b>Payment #:</b> ${record.tranid!""}</td>
        <td><b>Account:</b>
          ${record.account?replace("^\\S+\\s+", "", "r")
            ?replace(".*:\\s*", "", "r")
            ?replace("\\s-\\s[^-]+$", "", "r")!""}
        </td>
      </tr>

      <tr>
        <td><b>Payment Method:</b> ${record.paymentmethod!""}</td>
        <td><b>Currency:</b> ${record.currency!""}</td>
        <td><b>Exchange Rate:</b> ${(record.exchangerate!0)?string["0.00"]}</td>
        <td><b>Memo:</b> ${record.memo!""}</td>
      </tr>
    </table>

    <!-- ================= INVOICES ================= -->
    <table class="section">
      <thead>
        <tr>
          <th style="width:10%;">Apply</th>
          <th style="width:25%;">Invoice #</th>
          <th style="width:25%;">Invoice Date</th>
          <th style="width:40%;" class="right">Amount Paid</th>
        </tr>
      </thead>

      <tbody>
        <#if record.apply?has_content>

          <#list record.apply as invoice>

            <#if invoice.apply?is_boolean && invoice.apply>

              <tr>
                <td class="center">Applied</td>
                <td>${invoice.refnum!""}</td>
                <td>${invoice.applydate!""}</td>
                <td class="right">${invoice.amount?string["#,##0.00"]}</td>
              </tr>

            </#if>

          </#list>

        <#else>
          <tr>
            <td colspan="4" class="center">No invoices applied</td>
          </tr>
        </#if>
      </tbody>
    </table>

    <!-- ================= CREDITS ================= -->
    <#if record.credit?has_content>

      <table class="section">
        <thead>
          <tr>
            <th style="width:10%;">Apply</th>
            <th style="width:30%;">Credit #</th>
            <th style="width:30%;">Credit Date</th>
            <th style="width:30%;" class="right">Amount</th>
          </tr>
        </thead>

        <tbody>
          <#list record.credit as credit>

            <#if credit.apply?is_boolean && credit.apply>

              <tr>
                <td class="center">Applied</td>
                <td>${credit.refnum!""}</td>
                <td>${credit.applydate!""}</td>
                <td class="right">${credit.amount?string["#,##0.00"]}</td>
              </tr>

            </#if>

          </#list>
        </tbody>
      </table>

    </#if>

    <!-- ================= AMOUNT IN WORDS ================= -->
    <table class="section">
      <tr>
        <th>Amount in Words</th>
      </tr>
      <tr>
        <td style="height:50px;">
          ${record.custbody_amount_in_words!""}
        </td>
      </tr>
    </table>

    <!-- ================= FOOTER ================= -->
    <table class="no-border section">
      <tr>
        <td class="left">Page <pagenumber/> of <totalpages/></td>
        <td class="right">Printed: ${.now?string("dd-MMM-yyyy HH:mm:ss")}</td>
      </tr>
    </table>

  </body>
</pdf>