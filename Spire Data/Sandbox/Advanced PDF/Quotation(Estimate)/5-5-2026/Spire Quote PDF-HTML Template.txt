<pdf>
  <head>
    <style type="text/css">

      body {
        font-family: sans-serif;
        font-size: 9pt;
        margin: 0;
      }

      table {
        width: 100%;
        border-collapse: collapse;
        font-size: 8pt;
      }

      th, td {
        border: 0.2px solid #e0e0e0;
        padding: 3px 4px;
        font-size: 8pt;
      }

      th {
        background-color: #f9f9f9;
        font-weight: bold;
      }

      .no-border td,
      .no-border th {
        border: none;
      }

      .center { text-align: center; }
      .right { text-align: right; }
      .left { text-align: left; }
      .bold { font-weight: bold; }

      h2 {
        margin: 0;
        font-size: 14pt;
      }

      h3 {
        font-size: 9pt;
        margin-top: 10px;
        margin-bottom: 5px;
      }

      p {
        font-size: 8pt;
        margin: 0;
      }

    </style>
  </head>

  <body padding="10mm">

    <!-- Header Section -->
    <table class="no-border" style="margin-bottom: 8px;">
      <tr>
        <td style="width:70%;">
          <strong>${subsidiary.name!""}</strong><br/>
          ${subsidiary.mainaddress_text!""}<br/>
          TRN: ${subsidiary.federalidnumber}
        </td>
        <td style="width:30%; text-align:right;">
          <@filecabinet nstype="image" src="${subsidiary.logo}" style="width:140px; height:30;" />
        </td>
      </tr>
    </table>

    <!-- Title -->
    <table class="no-border" style="width:100%; margin-bottom:8px;">
      <tr>
        <td style="text-align:center; width:100%;">
          <h2>QUOTATION</h2>
        </td>
      </tr>
    </table>

    <!-- Billing / Shipping -->
    <table width="100%" style="margin-bottom:6px;">
      <tr>
        <th width="50%">Billing Address</th>
        <th width="50%">Shipping Address</th>
      </tr>
      <tr>
        <td width="50%" style="vertical-align:top;">
          <#if record.custbody_az_spg_reseller?has_content>
             ${record.billaddress!""}<br/>
             <b>TRN:</b> ${record.entity.vatregnumber!""}
          </#if>
        </td>
        <td width="50%" style="vertical-align:top;">
          <#if record.custbody_az_spg_enduser?has_content>
            <#assign raw = record.custpage_customer_contacts!"" />

<#if raw?has_content>
  <#assign data = raw?eval />
  <#assign customer = data.customer!{} />
  <#assign contacts = data.contacts![] />
<#else>
  <#assign customer = {} />
  <#assign contacts = [] />
</#if>

<!-- ================= CUSTOMER ================= -->
<b>${customer.name!""}</b><br/>
${customer.address!""}<br/>
<#if customer.website?has_content>
  <b>Website:</b> ${customer.website}<br/>
</#if>

<br/>

<!-- ================= CONTACTS ================= -->
  <b>Contacts</b>

  <table style="width:100%; border-collapse:collapse;">
    <tr>
      <th align="left">Name</th>
      <th align="left">Email</th>
      <th align="left">Phone</th>
    </tr>

<#if contacts?has_content>
    <#list contacts as c>
      <tr>
        <td>${c.name!""}</td>
        <td>${c.email!""}</td>
        <td>${c.phone!""}</td>
      </tr>
    </#list>
</#if>
  </table>
          </#if>
        </td>
      </tr>
    </table>

    <!-- Sales Rep -->
    <#assign salesRepName = "">
    <#if record.salesrep?has_content>
      <#assign salesRepParts = record.salesrep?string?split(" ")>
      <#if salesRepParts?size gt 1>
        <#assign salesRepName = salesRepParts[1..]?join(" ")>
      <#else>
        <#assign salesRepName = record.salesrep>
      </#if>
    </#if>

    <!-- Quote Info -->
    <table style="margin-bottom:5px;">
      <tr>
        <td><b>Document No:</b> ${record.tranid}</td>
        <td><b>Document Date:</b> ${record.trandate}</td>
        <td><b>Currency:</b> ${record.currency}</td>
        <td><b>Rate:</b> ${record.exchangerate?string["0.00"]!"-"}</td>
      </tr>
      <tr>
        <td colspan="2"><b>Terms:</b> ${record.terms}</td>
        <td><b>Validity:</b> ${record.expectedclosedate}</td>
        <td><b>Sales Rep:</b> ${salesRepName}</td>
      </tr>
    </table>

    <!-- Items -->
    <table style="margin-bottom:8px;">
      <thead>
        <tr>
          <th width="5%">#</th>
          <th width="20%">Item</th>
          <th width="25%">Description</th>
          <th width="5%">UOM</th>
          <th width="10%">Qty</th>
          <th width="10%" class="right">Rate</th>
          <th width="10%" class="right">VAT %</th>
          <th width="10%" class="right">VAT</th>
          <th width="10%" class="right">Amount</th>
        </tr>
      </thead>

      <tbody>
        <#list record.item as item>
        <tr>
          <td>${item_index + 1}</td>
          <td>${item.item!""}</td>
          <td>${item.description!""}</td>

          <td class="right">
            <#if item.itemtype != "Description">
              ${item.units!""}
            </#if>
          </td>

          <td class="right">
            <#if item.itemtype != "Description">
              ${item.quantity!""}
            </#if>
          </td>

          <td class="right">
            <#if item.itemtype != "Description">
              ${item.rate?string["#,##0.00"]}
            </#if>
          </td>

          <td class="right">
            <#if item.itemtype != "Description">
              ${(item.taxrate1 * 100)?string["#,##0.00"]}%
            </#if>
          </td>

          <td class="right">
            <#if item.itemtype != "Description">
              ${item.tax1amt?string["#,##0.00"]}
            </#if>
          </td>

          <td class="right">
            <#if item.itemtype != "Description">
              ${item.amount?string["#,##0.00"]}
            </#if>
          </td>

        </tr>
        </#list>
      </tbody>
    </table>

    <!-- Totals -->
    <table style="margin-bottom:8px;">
      <tr>
        <td style="width:60%; vertical-align:top;">
          <b>Amount in Words:</b><br/>
           ${record.currency.symbol!""} ${record.custbody_az_spg_amount_in_words!""}
        </td>

        <td style="width:40%;">
          <table class="no-border">
            <tr>
              <td>Net Total:</td>
              <td class="right">${record.currency.symbol!""}${record.subtotal?string["#,##0.00"]!"0.00"}</td>
            </tr>

            <tr>
              <td>VAT:</td>
              <td class="right">${record.currency.symbol!""}${record.taxtotal?string["#,##0.00"]!"0.00"}</td>
            </tr>

            <tr>
              <td><b>Gross Total:</b></td>
              <td class="right"><b>${record.currency.symbol!""}${record.total?string["#,##0.00"]!"0.00"}</b></td>
            </tr>
          </table>
        </td>
      </tr>
    </table>

    <!-- Billing Schedule -->
    <#if record.custbody_billing_schedule_summary?has_content>
      <b>BILLING SCHEDULE</b><br/>
      <pre style="font-family: monospace; font-size:8pt; line-height:10px;">
${record.custbody_billing_schedule_summary}
      </pre>
    </#if>


    <table class="no-border" style="width:100%; margin-top:5px;">
      <tr>
        <td align="right"><b>Created By:</b> ${record.custbody_az_spg_created_by?substring(record.custbody_az_spg_created_by?index_of(" ") + 1)!""}</td>
      </tr>
    </table>
              
    <!-- Terms -->
    <h3 style="text-transform:uppercase;">Terms and Conditions</h3>

    <table>
      <tr>
        <td style="padding:6px; text-align:justify;">
          ${record.custbody_az_spg_terms_conditions?replace("\n","<br/>")}
        </td>
      </tr>
    </table>

    <!-- Footer -->
    <p class="center" style="font-size:8pt;">
      Thank you for the opportunity to serve you!
    </p>

    <table class="no-border" style="width:100%; margin-top:5px;">
      <tr>
        <td class="left">Page <pagenumber/> of <totalpages/></td>
        <td align="right">Printed: ${.now?string("dd-MMM-yyyy HH:mm:ss")}</td>
      </tr>
    </table>

  </body>
</pdf>